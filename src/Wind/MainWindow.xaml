<Window x:Class="Wind.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:views="clr-namespace:Wind.Views"
        xmlns:converters="clr-namespace:Wind.Converters"
        mc:Ignorable="d"
        Title="Wind"
        Height="700" Width="1200"
        MinHeight="400" MinWidth="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        StateChanged="Window_StateChanged">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="36"
                      ResizeBorderThickness="4"
                      GlassFrameThickness="0"
                      CornerRadius="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisibility"/>
        <converters:ColorToBrushConverter x:Key="ColorToBrush"/>

        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#F1707A"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Tab Item Style -->
        <Style x:Key="TabItemStyle" TargetType="Border">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Margin" Value="1,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentFillColorDefaultBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsMultiSelected}" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentFillColorDefaultBrush}"/>
                    <Setter Property="Opacity" Value="0.7"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsTiled}" Value="True">
                    <Setter Property="BorderBrush" Value="{DynamicResource SystemFillColorSuccessBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border x:Name="MainBorder" BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title Bar with Tabs -->
            <Grid Grid.Row="0" Background="{DynamicResource ApplicationBackgroundBrush}"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Tabs Area -->
                <ScrollViewer Grid.Column="0"
                              HorizontalScrollBarVisibility="Auto"
                              VerticalScrollBarVisibility="Disabled"
                              WindowChrome.IsHitTestVisibleInChrome="True"
                              PreviewMouseLeftButtonDown="TabArea_PreviewMouseLeftButtonDown">
                    <StackPanel Orientation="Horizontal">
                        <!-- Tabs -->
                        <ItemsControl ItemsSource="{Binding Tabs}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource TabItemStyle}"
                                            Height="36"
                                            MouseLeftButtonDown="TabItem_MouseLeftButtonDown"
                                            MouseRightButtonUp="TabItem_MouseRightButtonUp"
                                            WindowChrome.IsHitTestVisibleInChrome="True"
                                            Tag="{Binding}">
                                        <Border.ContextMenu>
                                            <ContextMenu Tag="{Binding}">
                                                <MenuItem Header="タイル表示" Click="TileSelectedTabs_Click" Tag="{Binding}"/>
                                                <MenuItem Header="タイル解除" Click="StopTile_Click"
                                                          Visibility="{Binding IsTiled, Converter={StaticResource BoolToVisibility}}"/>
                                                <Separator/>
                                                <MenuItem Header="閉じる" Click="CloseTab_Click" Tag="{Binding}"/>
                                            </ContextMenu>
                                        </Border.ContextMenu>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <Image Source="{Binding Icon}" Width="16" Height="16"
                                                   Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding Title}" MaxWidth="120"
                                                       TextTrimming="CharacterEllipsis"
                                                       VerticalAlignment="Center"
                                                       FontSize="12"
                                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                                            <Button Background="Transparent" BorderThickness="0"
                                                    Padding="4" Margin="6,0,0,0"
                                                    Click="CloseTab_Click" Tag="{Binding}"
                                                    WindowChrome.IsHitTestVisibleInChrome="True"
                                                    Width="20" Height="20" Cursor="Hand"
                                                    Foreground="{DynamicResource TextFillColorSecondaryBrush}">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="Button">
                                                                    <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{DynamicResource ControlFillColorTertiaryBrush}"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                                <ui:SymbolIcon Symbol="Dismiss16" FontSize="9"/>
                                            </Button>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <!-- Add Window Button -->
                        <Button Content="+"
                                Click="AddWindowButton_Click"
                                WindowChrome.IsHitTestVisibleInChrome="True"
                                Width="36" Height="36"
                                FontSize="16" FontWeight="Light"
                                Background="Transparent" BorderThickness="0"
                                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                Cursor="Hand"
                                ToolTip="Add Window">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource ControlFillColorSecondaryBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </ScrollViewer>

                <!-- Window Control Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Style="{StaticResource TitleBarButtonStyle}" Click="SettingsButton_Click" ToolTip="Settings">
                        <ui:SymbolIcon Symbol="Settings24" FontSize="12"/>
                    </Button>
                    <Button Style="{StaticResource TitleBarButtonStyle}" Click="MinimizeButton_Click">
                        <ui:SymbolIcon Symbol="Subtract24" FontSize="10"/>
                    </Button>
                    <Button x:Name="MaximizeButton" Style="{StaticResource TitleBarButtonStyle}" Click="MaximizeButton_Click">
                        <ui:SymbolIcon x:Name="MaximizeIcon" Symbol="Maximize24" FontSize="10"/>
                    </Button>
                    <Button Style="{StaticResource CloseButtonStyle}" Click="CloseButton_Click">
                        <ui:SymbolIcon Symbol="Dismiss24" FontSize="10"/>
                    </Button>
                </StackPanel>
            </Grid>

            <!-- Content Area -->
            <Grid Grid.Row="1" Background="{DynamicResource ControlFillColorDefaultBrush}">
                <!-- Empty State -->
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center"
                            Visibility="{Binding SelectedTab, Converter={StaticResource NullToVisibility}, ConverterParameter=invert}"
                            IsHitTestVisible="False">
                    <ui:SymbolIcon Symbol="Window24" FontSize="48" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                    <TextBlock Text="Click '+' to add a window" FontSize="14" Margin="0,12,0,0"
                               Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                               HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Single Window Host Container -->
                <Border x:Name="WindowHostContainer" ClipToBounds="True" Padding="0" Margin="0"
                        Visibility="{Binding SelectedTab, Converter={StaticResource NullToVisibility}}">
                    <ContentControl x:Name="WindowHostContent"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch"
                                    VerticalContentAlignment="Stretch"
                                    Margin="0"
                                    Focusable="False"/>
                </Border>

                <!-- Tile Container -->
                <Grid x:Name="TileContainer" ClipToBounds="True"
                      Visibility="Collapsed"/>
            </Grid>

            <!-- Window Picker Overlay -->
            <Grid Grid.RowSpan="2" Background="#80000000"
                  Panel.ZIndex="100"
                  Visibility="{Binding IsWindowPickerOpen, Converter={StaticResource BoolToVisibility}}">
                <views:WindowPicker x:Name="WindowPickerControl"
                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                    Width="500" Height="400"/>
            </Grid>
        </Grid>
    </Border>
</Window>
